# 依赖包文件夹（核心排除项）
node_modules/

# npm 锁文件（可选，若团队统一规范可保留）
package-lock.json
yarn.lock

# 环境变量文件（存储敏感信息，如数据库密码、密钥等）
.env
.env.*
!.env.example

# 日志文件
logs/
*.log

# 编译产物（若有TypeScript等编译场景）
dist/
build/

# VS Code 编辑器配置（可选，若不需要共享编辑器配置）
.vscode/